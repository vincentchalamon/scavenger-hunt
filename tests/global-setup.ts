/**
 * Global setup for Playwright tests
 * No setup needed since we no longer use API keys
 */
async function globalSetup() {
  console.log('✅ Global setup complete - no API key required');
}

export default globalSetup;
